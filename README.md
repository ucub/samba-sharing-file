## samba-sharing-file : easy to sharing your local storage using docker compose with samba image from repo hub.docker.com

## Run with :
> ~# git clone https://github.com/ucub/samba-sharing-file.git
> ~# docker-compose up -d
